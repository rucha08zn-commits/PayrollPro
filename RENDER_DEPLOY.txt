Render settings

Language: Python
Branch: main
Root Directory: blank
Build Command: pip install -r requirements.txt
Start Command: gunicorn app:app --bind 0.0.0.0:$PORT
Plan: Free

No environment variables are required for the demo. SECRET_KEY is optional.
The app auto-migrates the SQLite schema and seeds demo data on a fresh database.
